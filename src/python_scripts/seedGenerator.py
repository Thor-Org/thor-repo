import pandas as pd
import numpy as np

# Returns data frame (df) and word list of all fields imported
def read_data():
    """
    Should read data from table in data base.
    Write another script that imports the data from and Ascii file,
    perform 'clean up tasks', and store in table in database
    """